<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.ma1co.pmcademo.app">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
        android:hardwareAccelerated="true">

        <uses-library android:name="com.sony.scalar.hardware.avio.cameracore" android:required="false" />
        <uses-library android:name="com.sony.scalar.sysutil" android:required="false" />
        <uses-library android:name="com.sony.wifi.direct" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <intent-filter>
                <action android:name="com.sony.scalar.app.action.MAIN" />
                <category android:name="com.sony.scalar.app.category.CAMERA_APP" />
            </intent-filter>
        </activity>

        <receiver android:name=".ExitCompletedReceiver">
            <intent-filter>
                <action android:name="com.sony.scalar.app.action.EXIT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>